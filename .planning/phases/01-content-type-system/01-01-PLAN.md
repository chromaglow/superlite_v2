---
phase: 01-content-type-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/config.ts
  - src/config.ts
autonomous: true
requirements:
  - TECH-01
  - TECH-02
  - TECH-03
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
  - CONTENT-04

must_haves:
  truths:
    - "WorkWithMePageContent type is exported from src/types/config.ts"
    - "workWithMePageContent is exported from src/config.ts with all four sections populated"
    - "opening field is string[] with exactly 7 items matching the locked beats verbatim"
    - "services field has exactly 4 ServiceItem entries with locked titles and descriptions"
    - "background field is a single string with the four sparse beats"
    - "cta field contains phone '(415) 335-9911', label 'Call or text', and phoneHref 'tel:+14153359911'"
    - "seo field uses SEOInfo type with title, description, and image fields"
  artifacts:
    - path: "src/types/config.ts"
      provides: "WorkWithMePageContent and ServiceItem type definitions"
      contains: "WorkWithMePageContent"
    - path: "src/config.ts"
      provides: "workWithMePageContent export with all content"
      exports: ["workWithMePageContent"]
  key_links:
    - from: "src/config.ts"
      to: "src/types/config.ts"
      via: "import type { WorkWithMePageContent } added to import block"
      pattern: "WorkWithMePageContent"
---

<objective>
Add the WorkWithMePageContent TypeScript type and the workWithMePageContent content export — the complete data layer for the /work-with-me consulting page.

Purpose: Phase 02 builds the Astro page against this content and type. All copy is locked in CONTEXT.md and planning context; this plan encodes it exactly.
Output: Two modified files — types/config.ts gains the type, config.ts gains the export.
</objective>

<execution_context>
@C:\Users\ezras\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ezras\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\ezras\OneDrive\Documents\work\GitHub\superlite_v2\.planning\PROJECT.md
@C:\Users\ezras\OneDrive\Documents\work\GitHub\superlite_v2\.planning\ROADMAP.md
@C:\Users\ezras\OneDrive\Documents\work\GitHub\superlite_v2\.planning\phases\01-content-type-system\01-CONTEXT.md

<interfaces>
<!-- Existing types in src/types/config.ts — executor reads the file first, then appends below these. -->

From src/types/config.ts (existing, DO NOT modify these):
```typescript
export type SEOInfo = {
  title: string;
  description: string;
  image: string;
};

export type BlogPageContent = {
  seo: SEOInfo;
  subtitle: string;
};
// (last type in file — append ServiceItem and WorkWithMePageContent after this)
```

From src/config.ts (existing import block — line 1–9):
```typescript
import type {
  NavBarLink,
  SocialLink,
  Identity,
  AboutPageContent,
  ProjectPageContent,
  BlogPageContent,
  HomePageContent,
} from "./types/config";
```
// WorkWithMePageContent must be added to this import list.

From src/config.ts (existing identity export):
```typescript
export const identity: Identity = {
  logo: "/superlite_v2/profile.jpg",
  // ...
};
```
// seo.image should use identity.logo — same pattern as all other pages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WorkWithMePageContent type to src/types/config.ts</name>
  <files>src/types/config.ts</files>
  <read_first>
    - src/types/config.ts — read the entire file to see the existing pattern before appending; the new types go at the very end of the file after BlogPageContent
  </read_first>
  <action>
Append two new exported types at the bottom of src/types/config.ts, after the BlogPageContent type.

First, append ServiceItem:

  export type ServiceItem = {
    title: string;
    description: string;
  };

Then append WorkWithMePageContent:

  export type WorkWithMePageContent = {
    seo: SEOInfo;
    opening: string[];
    services: ServiceItem[];
    background: string;
    cta: {
      phone: string;
      phoneHref: string;
      label: string;
    };
  };

No other types in the file should be modified. Follow the existing style: no blank line between the export keyword and the type keyword, two-space indentation for properties, properties terminated with semicolons.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('src/types/config.ts', 'utf8'); console.assert(c.includes('export type ServiceItem'), 'ServiceItem missing'); console.assert(c.includes('export type WorkWithMePageContent'), 'WorkWithMePageContent missing'); console.assert(c.includes('opening: string[]'), 'opening array missing'); console.assert(c.includes('services: ServiceItem[]'), 'services array missing'); console.assert(c.includes('background: string'), 'background missing'); console.assert(c.includes('phoneHref: string'), 'phoneHref missing'); console.log('types/config.ts assertions passed');"</automated>
  </verify>
  <acceptance_criteria>
    - src/types/config.ts contains `export type ServiceItem` with fields `title: string` and `description: string`
    - src/types/config.ts contains `export type WorkWithMePageContent` with fields: `seo: SEOInfo`, `opening: string[]`, `services: ServiceItem[]`, `background: string`, `cta: { phone: string; phoneHref: string; label: string; }`
    - No existing types were modified (BlogPageContent and earlier types unchanged)
    - The node assertion command exits without errors
  </acceptance_criteria>
  <done>Both ServiceItem and WorkWithMePageContent types are exported from src/types/config.ts; existing types are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add workWithMePageContent export to src/config.ts</name>
  <files>src/config.ts</files>
  <read_first>
    - src/config.ts — read the entire file; the import block is at lines 1–9, the new export goes at the end of the file after blogPageContent
    - src/types/config.ts — confirm WorkWithMePageContent and ServiceItem were added in Task 1 before proceeding
  </read_first>
  <action>
Two edits to src/config.ts:

EDIT 1 — Import block (lines 1–9): Add WorkWithMePageContent to the named import list from "./types/config". The import block becomes:

  import type {
    NavBarLink,
    SocialLink,
    Identity,
    AboutPageContent,
    ProjectPageContent,
    BlogPageContent,
    HomePageContent,
    WorkWithMePageContent,
  } from "./types/config";

EDIT 2 — Append new export at the bottom of the file, after the blogPageContent export. Add a blank line, then the section comment, then the export:

  // Work With Me (/work-with-me)
  export const workWithMePageContent: WorkWithMePageContent = {
    seo: {
      title: "Work With Me | Ezra Stjärna-Shively",
      description: "Design, product, operations, and AI/ML work. One principle: good design is as little design as possible.",
      image: identity.logo,
    },
    opening: [
      "I work from one principle: good design is as little design as possible.",
      "I've applied it to brand identities, products, and operations systems serving over a million businesses.",
      "The discipline is the same regardless of the medium.",
      "Start with the real problem.",
      "Include everyone it touches.",
      "Define success.",
      "Data drives the solution.",
    ],
    services: [
      {
        title: "Brand identity and website builds",
        description: "Strategy first: what does it need to say, look like, and accomplish. Everything follows from that — visual system, site design, e-commerce, fulfillment.",
      },
      {
        title: "Product design and user research",
        description: "Most product problems are definition problems — the wrong thing was built because the right question was never asked. I work at that layer: users, mental models, information architecture, interaction.",
      },
      {
        title: "Operations transformation",
        description: "I've redesigned systems that had to work for millions of businesses simultaneously. The method is the same at any scale: find the constraint, fix it at the source, measure the delta.",
      },
      {
        title: "AI products, automation, and machine learning",
        description: "The goal is always specific: reduce touches, improve forecast accuracy, create capacity, eliminate waste. Systems thinking maps where humans belong in each connected system — internal users, external customers, both.",
      },
    ],
    background: "Twenty years. Brand strategy, product design, and UX research. Amazon FBA and AWS — systems for over a million businesses. 600,000 labor hours removed through AI-enabled automation.",
    cta: {
      phone: "(415) 335-9911",
      phoneHref: "tel:+14153359911",
      label: "Call or text",
    },
  };

Exact string values are locked — do not paraphrase, reorder, or shorten any field.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('src/config.ts', 'utf8'); console.assert(c.includes('WorkWithMePageContent'), 'type import missing'); console.assert(c.includes('export const workWithMePageContent'), 'export missing'); console.assert(c.includes('I work from one principle: good design is as little design as possible.'), 'opening beat 1 missing'); console.assert(c.includes('Brand identity and website builds'), 'service 1 title missing'); console.assert(c.includes('AI products, automation, and machine learning'), 'service 4 title missing'); console.assert(c.includes('600,000 labor hours removed'), 'background missing'); console.assert(c.includes('(415) 335-9911'), 'phone display missing'); console.assert(c.includes('tel:+14153359911'), 'phoneHref missing'); console.assert(c.includes('Call or text'), 'cta label missing'); console.log('config.ts assertions passed');"</automated>
  </verify>
  <acceptance_criteria>
    - WorkWithMePageContent appears in the import block at the top of src/config.ts
    - workWithMePageContent is exported with type annotation WorkWithMePageContent
    - opening is string[] with exactly 7 elements matching the locked beats verbatim (first: "I work from one principle: good design is as little design as possible.", last: "Data drives the solution.")
    - services is an array of exactly 4 objects; titles are exactly: "Brand identity and website builds", "Product design and user research", "Operations transformation", "AI products, automation, and machine learning"
    - background is exactly: "Twenty years. Brand strategy, product design, and UX research. Amazon FBA and AWS — systems for over a million businesses. 600,000 labor hours removed through AI-enabled automation."
    - cta.phone is "(415) 335-9911", cta.phoneHref is "tel:+14153359911", cta.label is "Call or text"
    - seo.image references identity.logo (not a hardcoded string)
    - The node assertion command exits without errors
    - No existing exports (homePageContent, aboutPageContent, etc.) were modified
  </acceptance_criteria>
  <done>workWithMePageContent is exported from src/config.ts with all four sections, all copy verbatim, typed as WorkWithMePageContent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.ts → .astro template | Content strings from config are rendered into HTML; no user input crosses this boundary — all values are developer-authored at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | seo.description | accept | Static string authored by developer; no PII risk beyond what's already on the public site |
| T-01-02 | Tampering | config.ts content values | accept | File is in version control; no runtime mutation path; build-time only |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — type and content additions only, zero new dependencies |
</threat_model>

<verification>
After both tasks complete, run from the project root:

  node -e "
    const fs = require('fs');
    const types = fs.readFileSync('src/types/config.ts', 'utf8');
    const config = fs.readFileSync('src/config.ts', 'utf8');
    // Type assertions
    console.assert(types.includes('export type ServiceItem'), 'ServiceItem type missing');
    console.assert(types.includes('export type WorkWithMePageContent'), 'WorkWithMePageContent type missing');
    console.assert(types.includes('opening: string[]'), 'opening typed as string[] missing');
    console.assert(types.includes('services: ServiceItem[]'), 'services typed as ServiceItem[] missing');
    // Config assertions
    console.assert(config.includes('WorkWithMePageContent'), 'type not imported in config.ts');
    console.assert(config.includes('export const workWithMePageContent'), 'export missing');
    const openingBeats = [
      'I work from one principle: good design is as little design as possible.',
      \"I've applied it to brand identities, products, and operations systems serving over a million businesses.\",
      'The discipline is the same regardless of the medium.',
      'Start with the real problem.',
      'Include everyone it touches.',
      'Define success.',
      'Data drives the solution.',
    ];
    openingBeats.forEach((b, i) => console.assert(config.includes(b), 'opening beat ' + (i+1) + ' missing'));
    console.assert(config.includes('Brand identity and website builds'), 'service 1 missing');
    console.assert(config.includes('Product design and user research'), 'service 2 missing');
    console.assert(config.includes('Operations transformation'), 'service 3 missing');
    console.assert(config.includes('AI products, automation, and machine learning'), 'service 4 missing');
    console.assert(config.includes('600,000 labor hours removed'), 'background missing');
    console.assert(config.includes('(415) 335-9911'), 'phone missing');
    console.assert(config.includes('Call or text'), 'cta label missing');
    console.log('ALL ASSERTIONS PASSED');
  "
</verification>

<success_criteria>
Phase 01 is complete when:
1. WorkWithMePageContent type exists in src/types/config.ts covering all sections (seo, opening: string[], services: ServiceItem[], background, cta)
2. workWithMePageContent is exported from src/config.ts with complete content — all 7 opening beats verbatim, all 4 services verbatim, background verbatim, phone/label exact
3. The node verification command above exits with "ALL ASSERTIONS PASSED"
4. No existing types or exports in either file were modified
</success_criteria>

<output>
Create `.planning/phases/01-content-type-system/01-01-SUMMARY.md` when done.
</output>
